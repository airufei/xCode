<?xml version="1.0" encoding="utf-8"?>
<template>
	<name>dao</name>
	<filePath>src/${packageName}/adr/dao</filePath>
	<fileName>${ClassName}Dao.java</fileName>
	<content><![CDATA[
package ${packageName}.adr.dao;

import ${packageName}.adr.po.${ClassName};
import ${packageName}.adr.query.${ClassName}Query;
import java.util.List;

/**
 * ${functionName}DAO接口
 * @author ${functionAuthor}
 * @version ${functionVersion}
 */
public interface ${ClassName}Dao {
	
	/**
	 * 删除数据（逻辑删除）
	 * @param id
	 * @return
	 */
	 void delete(long id);
    /**
	 * 单条数据增加
	 * @param ${className}
	 * @return
	 */
	 int add(${ClassName} ${className});

    /**
	 * 批量数据增加
	 * @param list
	 * @return
	 */
	  int addRecordBatch(List<${ClassName}> list);

     /**
	 * 根据ID获取单条数据
	 * @param id
	 * @return
	 */
	  ${ClassName} get${ClassName}ById (long id);

      /**
	 * 修改单条数据
	 * @param ${className}
	 * @return
	 */
	   int updateById(${ClassName} ${className});

	  /**
	   * 获取分页数据
	   * @param ${className}Query
	   * @return
	   */
	    List<${ClassName}>  get${ClassName}ListByPage(${ClassName}Query ${className}Query);

	   
	   /**
	   * 获取集合数据，不带分页
	   * @param ${className}
	   * @return
	   */
	    List<${ClassName}>  get${ClassName}List(${ClassName} ${className});

	   /**
	   * 获取单条数据
	   * @param ${className}
	   * @return
	   */
	    ${ClassName}  getSignle${ClassName}(${ClassName} ${className});

	  /**
	   * 获取分页记录总数
	   * @param ${className}Query
	   * @return
	   */
	    Long  getTotalCount(${ClassName}Query ${className}Query);
	   
}]]>
	</content>
</template>