<?xml version="1.0" encoding="utf-8"?>
<template>
	<name>viewList</name>
	<filePath>src/main/webapp/WEB-INF/jsp/${modulePageName}/${className}</filePath>
	<fileName>list.jsp</fileName>
	<content><![CDATA[
<%@ page contentType="text/html;charset=UTF-8" %>
<%@ include file="/WEB-INF/jsp/admin/common/head.jsp"%>
<html>
<head>
	<title>${functionNameSimple}</title>
</head>
<body>
	<table width="99%">
		<tr>
			<td>
				<div style="height: auto; background: #fafafa; padding: 0px;"
					class="easyui-panel" title="${functionNameSimple}" iconCls="icon-search"
					collapsible="true" minimizable="false" maximizable="false"
					closable="false">
					<form id="searchForm" name="searchForm" method="post" action="">
						<div>
							<table width="99%" cellspacing="7">
								<tr>
									<td>
										<form id="searchForm">
											<table style="width:98%">
											<#assign length=0 />
											<#assign ban=0 />
											<#assign ban=queryCount />
											<#assign ban=ban/2 />
											 
											<#list colList as c>
				                              <#if (c.isQuery?? && c.isQuery == "1")>
				                                   <#assign length=length+1 /> <#--变量length复制-->
												   <#if length%2!=0>
													<tr>
													</#if>
													<td style="text-align:right;"> ${c.comments}：</td>
													<td style="width:35%;text-align:left;">
													<input  class="easyui-textbox" style="width:80%;height:32px" name="${c.javaFieldId}" maxlength="${c.dataLength}" />
													</td>
													<#if length%2==0>
													 <td style="width:16%;text-align:center;">
													 <#if length=queryCount>
													<input id="btnSubmit" class="btn btn-primary"
														type="button" onclick="serach()" value="查询" />
														 </#if>
													</td>
													</tr>		
											       </#if>
											        </#if>
											   </#list>
											   
											</table>
										</form>
									</td>
								</tr>
							</table>
						</div>
					</form>
				</div>
			</td>
		</tr>
		<tr>
			<td>
				<table id="dg" name="dg" title="${functionNameSimple}" iconCls="icon-list"
					width="100%" singleSelect="true" idField="id" loadMsg="请稍候......"
					pageList="[10,20,30,40,50,100,150,200]" rownumbers="true">
				</table>
			</td>
		</tr>
	</table>
	<div>
		<div id="addDialog" class="easyui-window" title=""
			style="width:600px;height:400px"
			data-options="iconCls:'icon-save',modal:true">
			
			<iframe scrolling="auto" id='openWindIframe' frameborder="0" src=""
				style="width: 100%; height: 100%;">	
			</iframe>
				
		</div>
	</div>

	<script type="text/javascript">
	$(document).ready(function() {
		serach();
		$('#addDialog').window('close'); // close a window
	});

	
	function serach() {
		var dataPara = getFormJson($("#searchForm")); 
		var url = '<%=request.getContextPath()%>/${subModuleName}/list';
		$('#dg').datagrid({
			url : url,
			type : "post",
			datatype : "json",
 			queryParams : dataPara, 
			loadMsg : "数据加载中，请稍后...",
			fitColumns: true,
	        pageSize: 10,
	        pagination: true,
	        striped: true,
	        collapsible: true,
	        remoteSort: true,
	        singleSelect: true,
			onloadsuccess : function(data) {
				conlose.log(data);
			},
			toolbar : [ {
				text : '添加',
				iconCls : 'icon-add',
				handler : function() {
					add();
				}
			} ],
			columns : [ [ 
			<#list colList as c>
			<#if (c.isList?? && c.isList == "1")>
			 {
			   <#if (c.javaFieldId?? && c.javaFieldId != "createtime")>
				field : '${c.javaFieldId}',
				 <#else>
				 field : 'createtimestr',
				</#if>
				title : '${c.comments}',
                width:100,
				align : 'left'
			},
			</#if>
			 </#list>
			{
			field: 'id',
			title: '操作',
			width: 100,
			align: 'center',
			formatter: function(value, row, index) {
				if (row!=null) 
				{
					var a = '<a href="javascript:void(0)" onclick="edit(this)">编辑</a> ';
                    var b='&nbsp;&nbsp;&nbsp;';
					var c = '<a href="javascript:void(0)" onclick="deleted(this)">删除</a>';
					return a + b+c;
				}
			  }
		    }
		  ]
		 ]
		});

		var p = $('#dg').datagrid('getPager');
		$(p).pagination({
			pageSize : 10,//每页显示的记录条数，默认为10
			pageList : [ 2, 5, 10, 15, 20 ],//每页显示几条记录
			beforePageText : '第',//页数文本框前显示的汉字 
			afterPageText : '页    共 {pages} 页',
			displayMsg : '当前显示 {from} - {to} 条记录    共 {total} 条记录',
			onBeforeRefresh : function() {
				$(this).pagination('loading');//正在加载数据中...
				$(this).pagination('loaded'); //数据加载完毕
			}
		});
	}
	
   function add() {
		var title='添加${functionNameSimple}';
		var url = '<%=request.getContextPath()%>/${subModuleName}/edit';
		var height=500;
		var width=700;
			ShowiframeWindow(url,title,width,height);
	}
	function edit(target) {
	    var index=getRowIndex(target);
		$('#dg').datagrid('selectRow', index);
		var row = $('#dg').datagrid('getSelected');
		var id=row.id;
		var title='编辑${functionNameSimple}';
		var url = '<%=request.getContextPath()%>/${subModuleName}/edit?id='+id;
		var height=500;
		var width=700;
			ShowiframeWindow(url,title,width,height);
	}
	
	 function deleted(target)
	 {
	      var index=getRowIndex(target);
		  $('#dg').datagrid('selectRow', index);
		  var row = $('#dg').datagrid('getSelected');
		  var id=row.id;
	      var url='<%=request.getContextPath()%>/${subModuleName}/delete?id='+id;
	      $.messager.confirm("操作提示", "您确定要执行操作吗？", function (data) {  
            if (data) {  
                postdata(url);  
            }  
        });  
	 }

     function postdata(url)
     {
        $.ajax({  
		        type: "POST",  
		        contentType : 'application/json;charset=utf-8', //设置请求头信息
		        url: url,  
		        async: false,   
		        dataType: "json",  
		        success: function (data) {  
		        	if(data!=null&&data!=undefined&&data!="undefined")
					{
						var msg=data.msg;
						$.messager.alert("操作提示", msg);  
						parent.$("#dg").datagrid('reload');
					}
		  
		        }  
		    });  
     }
	</script>
</body>
</html>]]>
	</content>
</template>